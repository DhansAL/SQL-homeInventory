import { TABLENAMES } from "./tableNames";

export default [
    TABLENAMES.item_type,
    TABLENAMES.manufacturer,
    TABLENAMES.address,
    TABLENAMES.state,
    TABLENAMES.country,
    TABLENAMES.shape,
    TABLENAMES.location,
    TABLENAMES.user

]